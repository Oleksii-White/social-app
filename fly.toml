app = 'portfolio-app-floral-glitter-7965'
primary_region = 'fra'

[build]
  dockerfile = 'Dockerfile'

[[services]]
  protocol = 'tcp'
  internal_port = 3000

  [[services.ports]]
    port     = 80         
    handlers = ['http']  

[[vm]]
  memory = '1gb'
  cpu_kind = 'shared'
  cpus = 1
